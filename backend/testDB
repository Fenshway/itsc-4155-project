import psycopg2

# Here you input the database data relating to YOUR local instance of the database
conn = psycopg2.connect(database="",
                        host="", # Could have set it as localhost
                        user="postgres", # Default is postgres
                        password="",
                        port="")

cursor = conn.cursor()
cursor.execute("INSERT INTO public.\"User\"(user_id, user_name, email, user_password, fav_game_id) VALUES (0, 'admin', 'admin@quest.com', 'password', NULL) ON CONFLICT DO NOTHING")
conn.commit()
cursor.execute("SELECT * FROM public.\"User\"")
print(cursor.fetchall())